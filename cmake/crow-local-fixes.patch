diff --git a/include/crow/http_connection.h b/include/crow/http_connection.h
index 64bbf07..fb7441e 100644
--- a/include/crow/http_connection.h
+++ b/include/crow/http_connection.h
@@ -410,14 +410,22 @@ namespace crow
             if (res.file_info.statResult == 0)
             {
                 std::ifstream is(res.file_info.path.c_str(), std::ios::in | std::ios::binary);
+                if (res.file_info.has_range)
+                    is.seekg(static_cast<std::streamoff>(res.file_info.offset));
                 std::vector<asio::const_buffer> buffers{1};
                 char buf[16384];
-                is.read(buf, sizeof(buf));
-                while (is.gcount() > 0)
+                std::uint64_t remaining = res.file_info.length;
+                while (is && (!res.file_info.has_range || remaining > 0))
                 {
-                    buffers[0] = asio::buffer(buf, is.gcount());
+                    const auto to_read = res.file_info.has_range && remaining < sizeof(buf)
+                        ? static_cast<std::streamsize>(remaining) : static_cast<std::streamsize>(sizeof(buf));
+                    is.read(buf, to_read);
+                    const auto count = is.gcount();
+                    if (count <= 0) break;
+                    buffers[0] = asio::buffer(buf, count);
                     do_write_sync(buffers);
-                    is.read(buf, sizeof(buf));
+                    if (res.file_info.has_range)
+                        remaining -= static_cast<std::uint64_t>(count);
                 }
             }
             if (close_connection_)
@@ -498,6 +506,23 @@ namespace crow
 
                   if (error_while_reading)
                   {
+                      if (self->parser_.body_too_large && self->adaptor_.is_open())
+                      {
+                          self->cancel_deadline_timer();
+                          self->close_connection_ = true;
+                          self->res = response(413, "Request body too large");
+                          self->complete_request();
+                          return;
+                      }
+                      if (self->parser_.upload_busy && self->adaptor_.is_open())
+                      {
+                          self->cancel_deadline_timer();
+                          self->close_connection_ = true;
+                          self->res = response(503, "Too many uploads in progress");
+                          self->res.add_header("Retry-After", "5");
+                          self->complete_request();
+                          return;
+                      }
                       self->cancel_deadline_timer();
                       self->parser_.done();
                       self->adaptor_.shutdown_read();
diff --git a/include/crow/http_response.h b/include/crow/http_response.h
index fffbffb..639c9ea 100644
--- a/include/crow/http_response.h
+++ b/include/crow/http_response.h
@@ -1,5 +1,6 @@
 #pragma once
 #include <string>
+#include <cstdint>
 #include <unordered_map>
 #include <ios>
 #include <fstream>
@@ -241,7 +242,8 @@ namespace crow
                 completed_ = true;
                 if (skip_body)
                 {
-                    set_header("Content-Length", std::to_string(body.size()));
+                    if (!headers.count("Content-Length"))
+                        set_header("Content-Length", std::to_string(body.size()));
                     body = "";
                     manual_length_header = true;
                 }
@@ -282,6 +284,9 @@ namespace crow
             std::string path = "";
             struct stat statbuf;
             int statResult;
+            std::uint64_t offset = 0;
+            std::uint64_t length = 0;
+            bool has_range = false;
         };
 
         /// Return a static file as the response body
@@ -318,6 +323,19 @@ namespace crow
             }
         }
 
+        /// Stream a validated byte range from a static file without buffering it.
+        void set_static_file_range_info_unsafe(std::string path, std::uint64_t offset, std::uint64_t length)
+        {
+            set_static_file_info_unsafe(std::move(path));
+            if (code == 200)
+            {
+                file_info.offset = offset;
+                file_info.length = length;
+                file_info.has_range = true;
+                set_header("Content-Length", std::to_string(length));
+            }
+        }
+
     private:
         bool completed_{};
         std::function<void()> complete_request_handler_;
diff --git a/include/crow/multipart.h b/include/crow/multipart.h
index f7b6c24..065b243 100644
--- a/include/crow/multipart.h
+++ b/include/crow/multipart.h
@@ -134,9 +134,9 @@ namespace crow
                     content_type = "multipart/form-data; boundary=" + boundary;
                 for (auto& item : parts)
                 {
-                    part_map.emplace(
-                      (get_header_object(item.headers, "Content-Disposition").params.find("name")->second),
-                      item);
+                    const auto& params = get_header_object(item.headers, "Content-Disposition").params;
+                    const auto name = params.find("name");
+                    if (name != params.end()) part_map.emplace(name->second, item);
                 }
             }
 
@@ -159,7 +159,7 @@ namespace crow
                 if (found != std::string::npos)
                 {
                     std::string to_return(header.substr(found + strlen(boundary_text)));
-                    if (to_return[0] == '\"')
+                    if (to_return.size() >= 2 && to_return[0] == '\"')
                     {
                         to_return = to_return.substr(1, to_return.length() - 2);
                     }
@@ -171,6 +171,7 @@ namespace crow
             void parse_body(std::string body, std::vector<part>& sections, mp_map& part_map)
             {
 
+                if (boundary.empty()) return;
                 std::string delimiter = dd + boundary;
 
                 // TODO(EDev): Exit on error
@@ -190,10 +191,12 @@ namespace crow
                     if (!section.empty())
                     {
                         part parsed_section(parse_section(section));
-                        part_map.emplace(
-                          (get_header_object(parsed_section.headers, "Content-Disposition").params.find("name")->second),
-                          parsed_section);
-                        sections.push_back(std::move(parsed_section));
+                        const auto& params = get_header_object(parsed_section.headers, "Content-Disposition").params;
+                        const auto name = params.find("name");
+                        if (name != params.end()) {
+                            part_map.emplace(name->second, parsed_section);
+                            sections.push_back(std::move(parsed_section));
+                        }
                     }
                 }
             }
@@ -203,11 +206,12 @@ namespace crow
                 struct part to_return;
 
                 size_t found = section.find(crlf + crlf);
+                if (found == std::string::npos || section.size() < found + 4) return to_return;
                 std::string head_line = section.substr(0, found + 2);
                 section.erase(0, found + 4);
 
                 parse_section_head(head_line, to_return);
-                to_return.body = section.substr(0, section.length() - 2);
+                to_return.body = section.size() >= 2 ? section.substr(0, section.length() - 2) : std::string();
                 return to_return;
             }
 
@@ -218,6 +222,7 @@ namespace crow
                     header to_add;
 
                     size_t found = lines.find(crlf);
+                    if (found == std::string::npos) break;
                     std::string line = lines.substr(0, found);
                     std::string key;
                     lines.erase(0, found + 2);
@@ -234,6 +239,7 @@ namespace crow
                         size_t header_split = header.find(": ");
                         key = header.substr(0, header_split);
 
+                        if (header_split == std::string::npos) continue;
                         to_add.value = header.substr(header_split + 2);
                     }
 
@@ -249,6 +255,7 @@ namespace crow
 
                         size_t param_split = param.find('=');
 
+                        if (param_split == std::string::npos) continue;
                         std::string value = param.substr(param_split + 1);
 
                         to_add.params.emplace(param.substr(0, param_split), trim(value));
diff --git a/include/crow/parser.h b/include/crow/parser.h
index 7099e8c..9fc0ea2 100644
--- a/include/crow/parser.h
+++ b/include/crow/parser.h
@@ -1,6 +1,7 @@
 #pragma once
 
 #include <string>
+#include <atomic>
 #include <unordered_map>
 #include <algorithm>
 
@@ -9,6 +10,12 @@
 
 namespace crow
 {
+    // Application-wide cap configured before the server starts. Checked while
+    // parsing so an oversized request never accumulates in request::body.
+    inline std::atomic<size_t> max_http_body_size{128 * 1024 * 1024};
+    inline std::atomic<size_t> max_concurrent_uploads{2};
+    inline std::atomic<size_t> active_uploads{0};
+
     /// A wrapper for `nodejs/http-parser`.
 
     ///
@@ -75,6 +82,29 @@ namespace crow
         static int on_headers_complete(http_parser* self_)
         {
             HTTPParser* self = static_cast<HTTPParser*>(self_);
+            if (self->content_length != CROW_ULLONG_MAX &&
+                self->content_length > max_http_body_size.load(std::memory_order_relaxed))
+            {
+                self->body_too_large = true;
+                return -1;
+            }
+            if (self->req.method == HTTPMethod::POST && self->req.url == "/upload")
+            {
+                size_t active = active_uploads.load(std::memory_order_relaxed);
+                while (active < max_concurrent_uploads.load(std::memory_order_relaxed))
+                {
+                    if (active_uploads.compare_exchange_weak(active, active + 1, std::memory_order_acq_rel))
+                    {
+                        self->upload_slot_ = true;
+                        break;
+                    }
+                }
+                if (!self->upload_slot_)
+                {
+                    self->upload_busy = true;
+                    return -1;
+                }
+            }
             if (!self->header_field.empty())
             {
                 self->req.headers.emplace(std::move(self->header_field), std::move(self->header_value));
@@ -88,6 +118,12 @@ namespace crow
         static int on_body(http_parser* self_, const char* at, size_t length)
         {
             HTTPParser* self = static_cast<HTTPParser*>(self_);
+            const size_t limit = max_http_body_size.load(std::memory_order_relaxed);
+            if (self->req.body.size() > limit || length > limit - self->req.body.size())
+            {
+                self->body_too_large = true;
+                return 1;
+            }
             self->req.body.insert(self->req.body.end(), at, at + length);
             return 0;
         }
@@ -105,6 +141,11 @@ namespace crow
             http_parser_init(this);
         }
 
+        ~HTTPParser()
+        {
+            release_upload_slot();
+        }
+
         // return false on error
         /// Parse a buffer into the different sections of an HTTP request.
         bool feed(const char* buffer, int length)
@@ -138,7 +179,10 @@ namespace crow
 
         void clear()
         {
+            release_upload_slot();
             req = crow::request();
+            body_too_large = false;
+            upload_busy = false;
             header_field.clear();
             header_value.clear();
             header_building_state = 0;
@@ -185,8 +229,19 @@ namespace crow
         ///
         /// Data parsed is put directly into this object as soon as the related callback returns. (e.g. the request will have the cooorect method as soon as on_method() returns)
         request req;
+        bool body_too_large = false;
+        bool upload_busy = false;
 
     private:
+        void release_upload_slot()
+        {
+            if (upload_slot_)
+            {
+                active_uploads.fetch_sub(1, std::memory_order_acq_rel);
+                upload_slot_ = false;
+            }
+        }
+        bool upload_slot_ = false;
         int header_building_state = 0;
         bool message_complete = false;
         std::string header_field;
